# --------------------------------------------
# Server Configuration (runtime)
# --------------------------------------------

# Server port
PORT=2567

# Environment mode (development | production)
NODE_ENV="development"

# Redis URL for horizontal scaling (optional)
# Leave empty for single server mode
# Format: redis://[username:password@]host:port
REDIS_URL="redis://game-redis.xxxxx.cache.amazonaws.com:6379"

# Allowed CORS origins (comma-separated)
# Required for production security
# Example: https://game.example.com,https://game.server.example.com
ALLOWED_ORIGINS="http://localhost:8080,http://localhost:2567"

# --------------------------------------------
# Client Configuration (build-time)
# --------------------------------------------

# Server address for client to connect
# Used during webpack build
# Example: game.example.com:2567 or game.example.com (port 443)
SERVER_URL="localhost:2567"
